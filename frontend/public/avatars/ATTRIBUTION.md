# 默认头像说明

`01.png`–`15.png` 由 3 行 × 5 列合图经 `scripts/split-avatar-sheet.py` 裁切生成。

重新裁切时，将合图放到本目录（仅保留一张非 `01`–`15` 的 `.png`，或传入文件名）后执行：

```bash
python scripts/split-avatar-sheet.py
```

可选参数见脚本内注释。

仅供「王师傅家常菜谱」项目内作为默认用户头像使用。
