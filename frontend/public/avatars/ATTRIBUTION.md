# 默认头像说明

`01.png`–`15.png` 由合图 `8acf409d-724f-4eb1-bc99-5b4b790b2c65.png`（3 行 × 5 列）自动裁切生成。

重新裁切：

```bash
python scripts/split-avatar-sheet.py
```

合图放在本目录且仅保留一张 `.png`（或指定文件名）即可。可选参数见脚本内注释。

仅供「王师傅家常菜谱」项目内作为默认用户头像使用。
